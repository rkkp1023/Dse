# foundation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build foundation` to build the library.

## Running unit tests

Run `nx test foundation` to execute the unit tests via [Vitest](https://vitest.dev/).
